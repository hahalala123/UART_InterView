================================================================
  高速串口警报实时处理系统技术方案
================================================================

1. 需求与指标定义
------------------

1.1 需求重述
  - 硬件: 两个高速串口(40 Mbaud), 各连接一个警报器; 警报时串口
    收到 1 字节报警数据。
  - 软件: 主线嵌入式 Linux(非 RT 补丁), 可修改内核/u-boot/设备树,
    系统上不运行其他业务程序。
  - 指标: 从"串口收到该字节"到"用户态调用 libfoo.so 中的处理函数"
    的延迟, 100~1000 次警报的平均值 < 1 us, 且分布尽量稳定。

1.2 延迟定义(必须先钉死, 否则无法验证)
  t0: 报警字节的停止位被 UART 采样完成、RBR 寄存器数据就绪的时刻。
      (测试时以警报器同步拉高的 GPIO 边沿 + 固定的线上串行化时间
      10 bit / 40 Mbaud = 250 ns 反推, 校准后 t0 为已知常量偏移。)
  t1: 用户态处理函数 foo() 第一条指令执行的时刻。
  指标: avg(t1 - t0) < 1 us; 用 p99/最大值 刻画"稳定"。

1.3 关键约束分析
  - "必须最终在用户态调用 libfoo.so" => 处理点只能在用户态,
    内核态方案(纯内核线程处理)不满足需求, 直接排除。
  - "主线 Linux, 平均 < 1 us" => 任何包含"中断 + 唤醒/信号 +
    调度"的路径物理上不可达(见第 2 节定量分析), 必须采用
    无系统调用、无调度的用户态忙轮询。

2. 方案选型与论证
------------------

2.1 候选方案对比

  方案 A: 内核串口中断 -> hardirq -> 给用户态发信号/唤醒
    路径: UART IRQ -> GIC -> hardirq 入口 -> 标记/唤醒 -> 调度器
    选中用户态线程 -> 返回用户态 -> 调用 foo()。
    定量: hardirq 入口 ~0.5~2 us; 唤醒+调度+返回用户态 >= 2 us。
    平均延迟 >= 3 us, 排除。

  方案 B: PREEMPT_RT 线程化中断
    不满足"主线内核"约束; 且 IRQ 线程唤醒延迟典型 2~5 us,
    平均 < 1 us 仍不可达, 排除。

  方案 C: 内核态完成全部处理
    无法调用用户态动态库 libfoo.so, 违反需求, 排除。

  方案 D: 用户态 read()/poll()/epoll() 轮询
    每次事件至少一次系统调用 + 可能的睡眠唤醒, 延迟 >= 1~5 us,
    平均值不可控, 排除。

  方案 E: 用户态 MMIO 忙轮询(本方案)
    用户态线程通过 mmap 直接读 UART 的 LSR/RBR 寄存器, 热路径上
    零系统调用、零调度、零中断。延迟只取决于 MMIO 读延迟和轮询
    周期, 可达亚微秒平均。 => 采用。

2.2 设计要点(决策记录)
  D1 数据通路: 用户态忙轮询 MMIO, 不走中断、不走 tty 层、不走
     系统调用。
  D2 内核几乎不改: 仅改设备树, 用主线已有的 uio_pdrv_genirq 驱动
     把 UART 寄存器映射进用户态(/dev/uioN), 不引入任何树外代码。
     标准 8250 串口驱动对这两个端口保持 status = "disabled",
     避免内核与用户态同时访问同一组寄存器。
  D3 CPU 独占: 专门核心跑轮询线程, 其余所有内核线程/中断全部
     迁走, 轮询线程永不进入睡眠。
  D4 时钟源: 用 ARM 架构计数器 CNTPCT(用户态 mrs 指令直接读,
     纳秒级), 不用 clock_gettime 系统调用。
  D5 降级通道: 保留一套内核中断驱动的慢路径作为诊断/兜底,
     不参与热路径, 不影响延迟指标。

3. 系统架构
-----------

  +-----------------------------------------------------------+
  |                     用户态                                |
  |  alarmd (SCHED_FIFO, 独占 CPU1)                           |
  |    +---------------------------------------------+        |
  |    | 轮询循环: LSR0/LSR1 -> RBR -> 时间戳 -> foo |        |
  |    +---------------------------------------------+        |
  |         | mmap (/dev/uio0, /dev/uio1)                     |
  +---------|-------------------------------------------------+
  | 内核态   v                                                |
  |  uio_pdrv_genirq (主线驱动, 仅提供 mmap, 不使用其中断)     |
  |  其余驱动全部裁剪/禁用; 串口 IRQ 不注册                   |
  +---------|-------------------------------------------------+
  | 硬件: UART0/UART1 (40 Mbaud)  <--- 警报器字节 + GPIO 标记  |
  +-----------------------------------------------------------+

4. 延迟预算
-----------

  环节                              预计耗时
  --------------------------------  ----------
  轮询发现(平均 = 半个轮询周期)      0.1~0.3 us
  读 RBR 取出数据字节               0.1~0.3 us
  读 CNTPCT 时间戳                  ~0.01 us
  PLT 跳转进入 foo() (已绑定/预热)  ~0.01 us
  --------------------------------  ----------
  合计(平均)                        ~0.3~0.6 us  < 1 us ✓

  最坏情况 = 一个完整轮询周期 + 两次 MMIO 读 + 调用入口,
  约 0.8~1.2 us, 由"稳定"指标(p99 < 2 us)覆盖。

  轮询周期估算: 循环体为一次 LSR 的 MMIO 读 + 一次分支,
  ARM SoC 上设备读典型 100~300 ns, 故空转周期 ~150~400 ns。

5. 详细设计
-----------

5.1 硬件与串口初始化
  - 40 Mbaud: 按 SoC UART 手册配置波特率发生寄存器(多数 SoC 支持
    自定义分频); 8N1 无校验。线上 1 字节 = 10 bit = 250 ns。
  - 关闭 RX DMA: 单字节事件下 DMA 搬运的启动开销大于收益。
  - 配置/关闭 RX 超时中断等无关功能; 使能 RBR 数据就绪位即可。
  - 测试配套: 警报器在发字节的同时拉高一个 GPIO, 作为示波器
    测量的 t0 物理标记。

5.2 u-boot 配置
  - CPU 频率锁定最高档(防止 cpufreq 切换引入的时延抖动)。
  - 关闭所有未使用外设的时钟与初始化, 减少总线竞争。

5.3 内核配置与启动参数
  bootargs 示例(2 核 SoC, CPU0 做内务, CPU1 专职轮询):
    isolcpus=1 nohz_full=1 rcu_nocbs=1 irqaffinity=0
    nmi_watchdog=0 transparent_hugepage=never
  内核配置:
    - CONFIG_CPU_FREQ 关闭(或 performance governor 且锁频);
    - CONFIG_FUNCTION_TRACER/KASAN/KGDB 等调试特性在量产配置关闭;
    - 裁剪文件系统与驱动, 除 uio 与必备驱动外尽量模块化/不加载。

5.4 设备树改动(内核唯一需要动的部分)
    uart0_poll: serial-poll@<uart0_base> {
        compatible = "generic-uio";
        reg = <0x<uart0_base> 0x100>;
    };
    uart1_poll: serial-poll@<uart1_base> { ... };
    /* 原 uart0/uart1 节点保持 status = "disabled" */
  uio_pdrv_genirq 会将 reg 区域映射为 /dev/uio0、/dev/uio1 的
  mmap 偏移 0(设备内存, 强序访问, 用户态直接可读)。

5.5 用户态程序 alarmd 设计

  初始化序列(只执行一次, 不进热路径):
    1) mlockall(MCL_CURRENT | MCL_FUTURE);   // 禁掉热路径页错误
    2) SCHED_FIFO 优先级 99; 绑定 CPU1;
    3) mmap /dev/uio0、/dev/uio1;
    4) 以 LD_BIND_NOW=1 启动或先调用一次 foo(), 消除 PLT 懒绑定,
       并把 foo 代码页/数据页预取进 cache、预触发缺页;
    5) 预分配环形缓冲(若需要缓存突发数据)。

  热路径伪代码:
    for (;;) {                               // 永不睡眠
        if (lsr(uart0) & DR) {               // MMIO 读, ~100-300ns
            byte = rbr(uart0);               // 取数据
            t    = cntpct();                 // 用户态 mrs, ~几 ns
            foo(0, byte, t);                 // 内联展开, PLT 已绑定
        }
        if (lsr(uart1) & DR) {
            byte = rbr(uart1);
            t    = cntpct();
            foo(1, byte, t);
        }
    }

  实现注意:
    - 寄存器指针必须 volatile(或用编译器屏障), 防止循环被优化掉;
    - 用户态时间戳读 ARMv8: asm volatile("mrs %0, cntpct_el0");
      若内核未开放 EL0 访问, 退化为 vDSO clock_gettime
      (CLOCK_MONOTONIC_RAW, ~20~30 ns, 仍远小于预算);
    - 双串口共用同一线程顺序轮询, 避免 libfoo 线程安全问题;
      每轮最坏轮询周期翻倍(~300~800 ns), 仍满足 < 1 us。
      (若 libfoo 线程安全且核数 >= 3, 可两核各跑一线程进一步
      压缩单口延迟, 见 8.2。)

5.6 突发与背压
  100~1000 次警报若间隔小于 foo() 执行时间, 热路径采用锁无关
  (单生产者单消费者)环形缓冲: 轮询只做"取字节+记时间戳+入队",
  消费侧顺序调 foo()。此时"处理延迟"定义为字节到达至入队完成的
  时间(仍 < 1 us), foo() 调用按到达顺序排队。方案默认按 foo()
  足够快(远小于警报间隔)设计, 并在验证阶段实测确认。

6. 实时性保障: 延迟来源逐项消除清单
-------------------------------------
  来源                措施
  ------------------  ----------------------------------------
  调度延迟            轮询线程永不睡眠 + SCHED_FIFO 99 + 独占核
  系统调用            热路径零 syscall (mmap MMIO + mrs)
  中断抢占            该核无 IRQ(irqaffinity=0 + 两串口不注册 IRQ)
  页错误              mlockall + 预热调用 foo()
  动态链接            LD_BIND_NOW / 预热解析 PLT
  cache 未命中        预热 + 热路径数据量极小, 常驻 L1/L2
  CPU 变频            u-boot 锁频 + 关闭 cpufreq
  CPU 深睡眠          忙轮询核不进入 idle
  RCU/nohz 回调       rcu_nocbs + nohz_full
  看门狗/NMI          nmi_watchdog=0
  THP/内存整理        transparent_hugepage=never, 预分配内存
  串口 IRQ 竞争       8250 驱动对该端口 disabled
  总线竞争            系统无其他业务, 关闭未用外设时钟

7. 测量与验证方案
-----------------

7.1 双通道测量
  通道一(硬件真值): 示波器同时采"警报器 GPIO 边沿"与"foo() 首条
    指令翻转的诊断 GPIO", 两者差值减去已标定的常量偏移(线缆 +
    250 ns 串行化), 得到真实端到端延迟。
  通道二(软件统计): 热路径中 CNTPCT 时间戳, 每次警报记录
    (t_foo_entry - t_byte_ready), 突发结束后输出
    avg / p99 / max / 直方图。

7.2 测试用例
  - 单发: 1 次警报, 测单次延迟分布;
  - 突发: 100 次、1000 次, 验证 avg < 1 us, p99 < 2 us, 无丢字节
    (计数与警报器发送计数比对);
  - 压力: 两路同时突发;
  - 长跑: 连续运行 24 h, 观察有无慢漂移(温度/变频)。

7.3 验收标准
  - 平均延迟 < 1 us;
  - p99 延迟 < 2 us(稳定性的操作性定义, 写入测试报告);
  - 零丢字节、零错序。

8. 风险与应对
-------------

8.1 MMIO 读延迟过高: 个别 SoC 的外设总线跨桥, 单次设备读可能
    接近或超过 1 us。应对: 选型阶段实测; 若超标, 用 FPGA/PL 在
    前端做字节锁存 + 硬件时间戳, 用户态仍只需读一个已稳定的
    寄存器(依旧主线 Linux 合规, 不引入 RT 内核)。

8.2 libfoo 线程安全未知: 默认单线程顺序处理两口; 若确认线程
    安全且核数足够, 升级为双线程双核, 单口平均延迟减半。

8.3 libfoo 执行时间过长(接近 1 us): 与需求方确认"处理"定义,
    必要时拆分为"入队 < 1 us + 异步调用"。

8.4 误码/线序等物理层问题: 校验位/示波器排查, 属硬件域, 软件
    方案不覆盖, 列入联调项。

9. 交付物清单
-------------
  - 修改后的设备树、内核配置、u-boot 环境;
  - alarmd 用户态程序(含上述伪代码对应的实现与预热逻辑);
  - 验证工具(GPIO 翻转打点 + 时间戳统计导出);
  - 延迟测试报告(含示波器截图与统计直方图)。
